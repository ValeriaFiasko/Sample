import {ObjectType} from "./object-type";

export class StatusCode {
constructor (id: number, code: string, objectType: ObjectType, objectTypeName: string, issueGrouping: boolean,
             createIncidents: boolean, status: string){}
}
